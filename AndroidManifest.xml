<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.allsensors"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="9"
        android:targetSdkVersion="18" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        
        <activity
        	android:name="deadreckoning.activity.MainActivity"		
        	android:label="AllSensors">
        	<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />   
        	</intent-filter>        
    	</activity>
    	
        <activity android:name="deadreckoning.activity.SecondActivity">
        </activity>
        
        <activity android:name="deadreckoning.activity.ThirdActivity">
        </activity>
        
        
    </application>

</manifest>
